pub mod utils;

pub mod algorithms {
    pub mod algo_store;
    pub mod ceaser_cipher;
}

pub use crate::algorithms::*;
// pub use utils::*;

// TODOs :
// for RSA ask if he already have a public key or want to generate new pairs
